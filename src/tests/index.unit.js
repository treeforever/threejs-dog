import test from "ava";
import rule3 from "../index";

test("rule3", t => {
  var xTarget = 0;
  var windowHalfX = 400;
  // var result = rule3(xTarget, 37.5, 62.5, 0, windowHalfX / 2);
  t.is(0, 400);
});
